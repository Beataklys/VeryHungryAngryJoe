
<%= simple_form_for(@order, remote: true) do |f| %>

  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>
        <ul>
          <% order.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
    </div>
  <% end %>

  <%= f.input :restaurant_name %>
  <%= f.input :dish_name %>
  <%= f.input :order_status, collection: %w(Active Finalized Ordered Delivered) %>
  <%= f.input :price %>
  <%= f.button :submit, class: "btn btn-primary"  %>

  <!-- <div class="field">
    <%= f.label :restaurant_name %>
    <%= f.text_field :restaurant_name %>
  </div>

  <div class="field">
    <%= f.label :dish_name %>
    <%= f.text_field :dish_name %>
  </div>

  <div class="field">
    <%= f.label :order_status %>
    <%= f.text_field :order_status %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.number_field :price %>
  </div>
</br>
  <div class="actions">
    <%= f.submit %>
  </div> -->
<% end %>
